name = "taxvault-anthropic-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

[vars]
ENFORCE_ORIGIN = "true"

# After deploying, set your API key as a secret (never in this file):
# wrangler secret put ANTHROPIC_API_KEY
